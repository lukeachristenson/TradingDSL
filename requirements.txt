pandas>=1.3.0
yfinance>=0.1.70
numpy>=1.20.0
tqdm>=4.60.0
TA-Lib>=0.4.24 # Note: requires separate C library installation
# If TA-Lib installation is problematic, the script has a fallback implementation
