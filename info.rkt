#lang info

(define name "trading-dsl")

(define deps '("base"
               "rackunit-lib"
               "syntax-spec-v3"))

(define license 'MIT)

(define scribblings '(("scribblings/main.scrbl" () (experimental) "trading-dsl")))